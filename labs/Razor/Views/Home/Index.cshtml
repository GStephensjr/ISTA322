@model Product[]
@{
    ViewBag.Title = "Product Name";
}


    <table>
        <thead>
            <tr><th>Name</th><th>Price</th></tr>
        </thead>
        <tbody>
            @foreach (Product p in Model)
            {
                <tr>
                    <td>@p.Name</td>
                    <td>@($"{p.Price:C2}")</td>
                </tr>
            }
        </tbody>
    </table>

@*<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div data-productid="@Model.ProductID" data-stocklevel="@ViewBag.StockLevel" class="product">
        <h1>MY WONDERFUL PRODUCT!!!</h1>
        <p>Product Name: @Model.Name</p>
        <p>Product Price: @($"{Model.Price:C2}")</p>
        @if (ViewBag.StockLevel == 0)
        {
            @:Out of Stock
        }
        else if (ViewBag.StockLevel > 0 && ViewBag.StockLevel <= 3)
        {
            <b>Low Stock (@ViewBag.StockLevel)</b>
        }
        else
        {
            @: @ViewBag.StockLevel in Stock
        }
    </div>
</body>
</html>*@